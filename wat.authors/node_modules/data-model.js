/*

Implement ColdMUD style data model.

Usage:

    var data-model = require('data-model');

    var newInstance = data-model.create(parents);

    newInstance.addProp(pName, parent, default);

    newInstance.addMethod(methodName, code);

    newInstance.methodName(foo, bar, ...);

Inside methodName pName has accessor functions which ... something

*/
