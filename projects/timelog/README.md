# Time logging via CLI or hand-edited file

# Commands

```shell

    # note current activity
    I am taskAlias

    # create a task alias
    I am newAlias = description

    # Note prior change in activity
    I was taskAlias at moment

```
