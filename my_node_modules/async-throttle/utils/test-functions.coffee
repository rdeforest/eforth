# Used by files in ../test

module.exports =
  syncFn    : (args...) ->
    return args

  asyncFn   : (args..., cb) ->
    setTimeout (->
      cb args...
    ), 100

  promiseFn : (args...) ->
    new Promise (resolve, reject) ->
      setTimeout (->
        resolve args
      ), 100


