try
  throttle = require '..'

  doWhenever = (fn) -> setTimeout fn, Math.random() * 1000 + 500

  q = throttle.makeQueueFn()

assert = require 'assert'

assert throttle
assert doWhenever
assert q

eventually = (fn, verb, more...) ->
  happened = false
  watcher = new Promise (resolve, reject) ->


